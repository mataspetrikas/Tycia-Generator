{
  "name": "tycia-generator",
  "version": "1.0.0",
  "description": "Tycia image generator",
  "main": "generate-images-json.js",
  "scripts": {
    "generate-images": "node scripts/generate-images-json.js",
    "generate-images:replace": "node scripts/generate-images-json.js --replace",
    "classify-images": "node scripts/classify-images.js",
    "classify-clip": "node scripts/classify-clip.js",
    "update": "npm run generate-images && npm run classify-clip"
  },
  "dependencies": {
    "@tensorflow-models/mobilenet": "^2.1.0",
    "@tensorflow/tfjs-node": "^4.15.0",
    "@xenova/transformers": "^2.17.0",
    "image-size": "^1.0.0",
    "jpeg-js": "^0.4.4",
    "sharp": "^0.33.0"
  }
}
